version: '3.8'

services:
  bot:
    build: .
    image: dam245222/ranchbot:${IMAGE_TAG}
    environment:
      ES_HOST: ${ES_HOST}
      ES_USER: ${ES_USER}
      ES_PASS: ${ES_PASS}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      DEFAULT_ADMIN: ${DEFAULT_ADMIN}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_SCHEMA: ${POSTGRES_SCHEMA}
      BOT_USERNAME: ${BOT_USERNAME}
      SPECIALIZED_TABLE: ${SPECIALIZED_TABLE}
      ES_TRANSCRIPTION_INDEX: ${ES_TRANSCRIPTION_INDEX}
      ENABLE_TELEGRAM: ${ENABLE_TELEGRAM:-true}
      ENABLE_REST: ${ENABLE_REST:-false}
      ENABLE_SIGNAL: ${ENABLE_SIGNAL:-false}
      SIGNAL_PHONE_NUMBER: ${SIGNAL_PHONE_NUMBER:-}
      SIGNAL_API_URL: ${SIGNAL_API_URL:-http://localhost:8473}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      REST_API_PORT: ${REST_API_PORT:-8541}
      DISABLE_RATE_LIMITING: ${DISABLE_RATE_LIMITING:-false}
      INLINE_CACHE_CHANNEL_ID: ${INLINE_CACHE_CHANNEL_ID}
      VIDEO_DATA_DIR: ${VIDEO_DATA_DIR:-/app/bot/RanchBotData}
      VLLM_HOST: ${VLLM_HOST:-http://localhost:11435}
      VLLM_EMBEDDINGS_MODEL: ${VLLM_EMBEDDINGS_MODEL:-qwen3vl-embed}
    restart: ${RESTART_POLICY:-unless-stopped}
    volumes:
      - /mnt/WD_RED_2TB_MIRROR/RanchBot:/app/bot/RanchBotData:ro
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: false
    tmpfs:
      - /tmp
    network_mode: host
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${REST_API_PORT:-8541}/"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
